# Laravel WordPress Skill

Use this package as a Laravel service layer above `jooservices/wordpress-sdk`. Prefer grouped services, `ResourceDefinition` adapters, explicit sync state, and Laravel Storage for media files. Do not add web/API layers or background processing.